pre-push:
    commands:
        clean:
            run: rimraf ./dist
        pre-compile:
            run: tsc --noEmit --project tsconfig.dist.json
        lint:
            run: eslint {src,tests}/**/*.ts
        test:
            run: c8 node --loader ts-node/esm ./tests/index.ts
        document:
            run: typedoc
